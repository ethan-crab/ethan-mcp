runtime: "container"

build:
  dockerfile: "Dockerfile"       # Path to your Dockerfile
  dockerBuildPath: "."           # Docker build context

startCommand:
  type: "http"
  command: "python main.py"      # Command to start your FastAPI + MCP app

